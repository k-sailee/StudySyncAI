# Backend environment example
# Server
PORT=5001

# Firebase
FIREBASE_PROJECT_ID=studysyncai-82ca9
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# OpenRouter (if used)
OPENROUTER_API_KEY=

# SMTP (required to send real emails in production)
# Example (SendGrid / SMTP provider)
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
# Optional: override receiver address (defaults to p22724439@gmail.com)
SUPPORT_RECEIVER=p22724439@gmail.com
